{
  "name": "resume-builder",
  "version": "1.0.0",
  "scripts": {
    "export": "resume export -t stackoverflow -f html build/index.html",
    "generate-pdf": "resume export -t stackoverflow -f pdf build/resume.pdf",
    "setup": "mkdir -p build",
    "validate": "resume validate"
  },
  "dependencies": {
    "jsonresume-theme-stackoverflow": "^2.0.2",
    "resume-cli": "^3.0.8"
  }
}
